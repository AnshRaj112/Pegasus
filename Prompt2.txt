Pegasus Polars-based reconciliation for 10M–100M row CSV files should stay fast and memory-bounded.

Redesign or extend the architecture for high-performance analytical reconciliation.

Critical architecture ideas:

1. CSV as ingestion format only — convert to Parquet where it helps spill/sort paths; keep streaming ingestion.

2. Partitioned reconciliation — partition_id = hash(uid) % N; compare partitions independently; avoid giant global joins.

3. Vectorized execution — Polars lazy/streaming, hash-partition spill, partition comparator; avoid Python row-by-row loops.

4. Streaming + external memory — lazy scans, spill-to-disk, bounded RAM.

5. UID hashing — fast deterministic hashing (current SHA-based paths); optional faster hashes if profiled.

6. Mismatch handling — incremental NDJSON / chunked frames; API returns samples + counts.

7. Query / scan optimization — early filters, column pruning, compare only required columns, partition stats where useful.

Relevant modules (names may evolve): PartitionManager, PartitionComparator, ReconciliationCoordinator, streaming collectors, performance monitoring.

Constraints: bounded memory, temp-file cleanup, metrics. No distributed Spark in this prompt.

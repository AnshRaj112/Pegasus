# Production / preview: UI on :5173, API on :8000 (browser calls API directly — CORS must allow this origin on the backend)
# VITE_API_BASE=http://127.0.0.1:8000

# Optional: client-side poll timeout in ms for long validation jobs (0 = no timeout).
# VITE_VALIDATION_POLL_TIMEOUT_MS=0

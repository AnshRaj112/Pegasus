Pegasus now needs to support large-scale external-memory CSV reconciliation for files up to 100GB.

Redesign the comparison architecture with the following requirements:

Core Goals:

* memory efficient
* streaming-first
* external-memory reconciliation
* scalable to 100GB+
* support multiple source and target files

Required Comparison Strategies:

1. Ordered stream comparison for sorted files
2. Sliding window comparison for mostly sorted files
3. Hash partition comparison for unordered files
4. External merge-sort comparison for huge unsorted files

Requirements:

* Use Polars where appropriate
* Avoid loading entire files into memory
* Use chunk-based processing
* Implement configurable chunk/window sizes
* Support spill-to-disk architecture
* Add temp-file management
* Add deterministic UID generation
* Support partition-based reconciliation
* Design for future distributed execution

Create modular architecture for:

* stream readers
* partition managers
* merge sort engine
* window comparator
* mismatch collector
* reconciliation coordinator

Add:

* production-grade logging
* metrics hooks
* retry-safe processing
* resumable execution design
* clean interfaces
* type hints
* docstrings

Do NOT implement Kubernetes, Spark, or GCS yet.
Focus only on the core external-memory reconciliation engine.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pegasus-frontend
  namespace: pegasus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pegasus-frontend
  template:
    metadata:
      labels:
        app: pegasus-frontend
    spec:
      containers:
        - name: frontend
          image: pegasus-frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: pegasus-frontend
  namespace: pegasus
spec:
  selector:
    app: pegasus-frontend
  ports:
    - port: 80
      targetPort: 80

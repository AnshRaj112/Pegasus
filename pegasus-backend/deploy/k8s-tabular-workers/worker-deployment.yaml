apiVersion: apps/v1
kind: Deployment
metadata:
  name: pegasus-worker
  namespace: pegasus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pegasus-worker
  template:
    metadata:
      labels:
        app: pegasus-worker
    spec:
      containers:
        - name: worker
          image: pegasus-backend:latest
          command: ["python", "-m", "pegasus.validation.distributed_worker_main"]
          envFrom:
            - configMapRef:
                name: pegasus-config
          volumeMounts:
            - name: work-data
              mountPath: /data/category1
            - name: validation-jobs
              mountPath: /data/validation_jobs
          resources:
            requests:
              cpu: 2
              memory: 4Gi
              ephemeral-storage: 50Gi
            limits:
              cpu: 4
              memory: 10Gi
              ephemeral-storage: 200Gi
      volumes:
        - name: work-data
          persistentVolumeClaim:
            claimName: pegasus-work-data
        - name: validation-jobs
          emptyDir:
            sizeLimit: 200Gi

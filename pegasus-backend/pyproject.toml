[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "pegasus-backend"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "pegasus_native"
manifest-path = "native/Cargo.toml"
